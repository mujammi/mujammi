/*
9.A find the sum of didgit of given number
*/
#include <stdio.h>

void main() {
    int i,n,m;
    int sum=0;
    printf("enter number:");
    scanf("%d",&n);
    for(i=1;n!=0;i++)
    {
        m=n%10;
        sum=sum+m;
        n=n/10;
    }
    printf("sum=%d",sum);

    
}
